@model IEnumerable<PhysicManagement.Model.Resident>

@{
    ViewBag.Title = "لیست";
}

<div class="row">
    <div class="col-lg-12 col-md-12">
        <div class="card">
            <div class="card-header card-header-info">
                <h4 class="card-title">لیست رزیدنتها</h4>
                <p class="card-category">
                    فقط رزیدنت های فعال در لیست نمایش داده شده است
                </p>
            </div>
            <div class="card-body table-responsive">
                <table class="table table-hover">
                    <thead class="text-warning">
                        <tr>
                            <th>ردیف</th>
                            <th>نام کاربری </th>
                            <th>نام</th>
                            <th>نام خانوادگی</th>
                            <th>تخصص</th>
                            <th style="width:50px;">امکانات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int Index = 1;
                            foreach (var resident in Model)
                            {
                                <tr>
                                    <td>@Index</td>
                                    <td>@resident.Username</td>
                                    <td>@resident.FirstName</td>
                                    <td>@resident.LastName</td>
                                    <td>
                                        <div class="btn-group">
                                            <button type="button" class="btn btn-sm dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                امکانات
                                            </button>
                                            <div class="dropdown-menu dropdown-menu-left">
                                                <a class="dropdown-item" href="Modify/@resident.Id">ویرایش</a>
                                                <div class="dropdown-divider"></div>
                                                <a class="dropdown-item" href="Modify/@resident.Id"> حذف</a>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                                Index++;
                            }

                        }


                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

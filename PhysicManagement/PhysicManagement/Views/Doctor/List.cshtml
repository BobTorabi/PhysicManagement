@model List<PhysicManagement.Model.Doctor>
@{
    ViewBag.Title = "لیست";
}

<div class="row">
    <div class="col-lg-12 col-md-12">
        <div class="card">
            <div class="card-header card-header-info">
                <h4 class="card-title">لیست دکترها</h4>
                <h3>
                    <a class="btn btn-sm bg-light text-dark" href="Modify">افزودن جدید</a>
                </h3>
            </div>
            <div class="card-body table-responsive">
                <table class="table table-hover">
                    <thead class="text-warning">
                        <tr>
                            <th>ردیف</th>
                            <th>نام کاربری </th>
                            <th>نام</th>
                            <th>نام خانوادگی</th>
                            <th>تخصص</th>
                            <th style="width:50px;">امکانات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int Index = 1;
                            foreach (var doctor in Model)
                            {
                                <tr>
                                    <td>@Index</td>
                                    <td>@doctor.Username</td>
                                    <td>@doctor.FirstName</td>
                                    <td>@doctor.LastName</td>
                                    <td>@doctor.Degree</td>
                                    <td>
                                        <div class="btn-group">
                                            <button type="button" class="btn btn-sm dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                امکانات
                                            </button>
                                            <div class="dropdown-menu dropdown-menu-left">
                                                <a class="dropdown-item" href="Modify/@doctor.Id">ویرایش</a>
                                                <div class="dropdown-divider"></div>
                                                <a class="dropdown-item" href="Modify/@doctor.Id"> حذف</a>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                                Index++;
                            }

                        }


                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>